# TODO — Kora-Contract Shared Contracts Optimization

- [ ] Update `contracts/shared/src/reentrancy.rs` to remove the duplicated `ReentrancyGuard` definition (keep canonical version).
- [ ] Run `cargo test -p kora-shared` to verify compilation and passing tests.

